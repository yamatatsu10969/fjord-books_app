<%= form_for(current_user.active_follows.build) do |f| %>
  <div><%= hidden_field_tag :followee_id, @user.id %></div>
  <%= f.submit t('views.users.show.follow'), class: "btn btn-primary" %>
<% end %>