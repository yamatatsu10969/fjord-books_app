<%= form_for(current_user.active_follows.find_by(followee_id: user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit t('views.users.show.unfollow'), class: "btn btn-default" %>
<% end %>